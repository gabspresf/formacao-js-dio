// lista é uma coleção de dados
// armazenar em uma variável só um conjunto de dados
// posição sempre começa no 0
const alunos = ["joão", "marina", "vitor"];

// adicionando item na lista:

// adicionando dinamicamente no final da lista
alunos.push("gabriela");

// adicionando diretamente a posição
alunos[4] = "mariana";

//imprimindo item da lista:
console.log(alunos);

// lista vazia:
// *posso ter tipos diferentes dentro da lista*:
// const alunos = [];

// alunos.push('teste');

// alunos.push(10);

// remover da posição (remove o último item):
// alunos.pop();

// remover da posição (remove o primeiro item):
// alunos.shift();
